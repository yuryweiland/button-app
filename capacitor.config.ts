import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.button.app',
  appName: 'ButtonApp',
  webDir: 'dist/button-app',
  bundledWebRuntime: false
};

export default config;
